services:
  monitor:
    image: uptime-monitor:local
    container_name: uptime-monitor
    ports:
      - "8080:8080"
    volumes:
      - ./config:/config:ro
      - ./data:/data
      - ./tailscale-state:/var/lib/tailscale
    environment:
      - DASHBOARD_PASSWORD=${DASHBOARD_PASSWORD:-changeme}
      - POLL_INTERVAL=${POLL_INTERVAL:-30s}
      - LOG_LEVEL=${LOG_LEVEL:-info}
      # Local collector: to monitor the HOST (not the container), uncomment
      # HOST_PROC and mount /proc read-only, and add `pid: host` under this
      # service. See README "Monitoring localhost".
      - HOST_PROC=${HOST_PROC:-}
      # Tailscale configuration (optional)
      - TS_AUTHKEY=${TS_AUTHKEY:-}
      - TS_HOSTNAME=${TS_HOSTNAME:-uptime-monitor}
      - TS_ROUTES=${TS_ROUTES:-}
      - TS_EXTRA_ARGS=${TS_EXTRA_ARGS:-}
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    devices:
      - /dev/net/tun:/dev/net/tun
    network_mode: host
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

# Alternative: Tailscale sidecar pattern (uncomment and comment out monitor's Tailscale env vars above)
# profiles:
#   tailscale-sidecar:
#     services:
#       tailscale:
#         image: tailscale/tailscale:latest
#         container_name: uptime-monitor-tailscale
#         cap_add:
#           - NET_ADMIN
#         devices:
#           - /dev/net/tun:/dev/net/tun
#         volumes:
#           - ./tailscale-state:/var/lib/tailscale
#         environment:
#           - TS_AUTHKEY=${TS_AUTHKEY}
#           - TS_HOSTNAME=uptime-monitor-sidecar
#       monitor:
#         environment:
#           - TS_SOCKS5_PROXY=tailscale:1055
#         depends_on:
#           - tailscale